apiVersion: v1
kind: Service
metadata:
  name: donation-processor-service
  namespace: hackathon-9nett
spec:
  selector:
    app: donation-processor
  ports:
    - name: http
      port: 8080
      targetPort: 8080